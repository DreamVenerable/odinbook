<!DOCTYPE html>
<html>
  <head>
    <title>Odinbook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="antialiased text-slate-500 dark:text-slate-400 bg-white dark:bg-slate-900">
    <nav class="sticky top-0 z-40 w-full backdrop-blur flex-none transition-colors duration-500 lg:z-50 dark:border-slate-50/[0.06] bg-white/95 dark:bg-transparent border-b mb-10">
      <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">

        <%= link_to "OdinBook", root_path, class: "link-primary" %>

        <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-sticky">
          <%= link_to "New Post", new_post_path, class: "btn-primary mx-10" %>
          <ul class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-gray-100 rounded-lg bg-gray-50 md:space-x-8 rtl:space-x-reverse md:flex-row md:mt-0 md:border-0 md:bg-white dark:bg-gray-800 md:dark:bg-gray-900 dark:border-gray-700">

            <% if user_signed_in? %>
              <li>
                <%= link_to "Profile", edit_user_registration_path, class: "link-primary" %>
              </li>
              <li>
                <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "link-primary" %>
              </li>
            <% else %>
              <li>
                <%= link_to "Login", new_user_session_path, class: "link-primary" %>
              </li>
              <li>
                <%= link_to "Sign Up", new_user_registration_path, class: "link-primary" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <main class="flex-grow flex flex-col items-center justify-center min-h-screen">
      <div class="flex-1 w-1/2">
        <%= yield %>
      </div>
    </main>

    <footer class="bg-white rounded-lg shadow m-4 dark:bg-gray-800">
      <div class="w-full mx-auto max-w-screen-xl p-4 md:flex md:items-center md:justify-between">
        <span class="text-sm text-gray-500 sm:text-center dark:text-gray-400">© 2023 <%= link_to "OdinBook", root_path%>. All Rights Reserved.
        </span>
      </div>
    </footer>

  </body>
</html>
